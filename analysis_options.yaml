# Strict lint set for the package. Zero issues is the bar.
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # A2-level docs are required on every public member; keep it an error.
    public_member_api_docs: error
